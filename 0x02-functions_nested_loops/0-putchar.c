#include <stdio.h>

int main() 
{
	/*Write C code here*/
	 int ch;
	putchar(ch);
	ch ='_';
	putchar(ch);
	ch = 'p';
	putchar(ch);
	ch = 'u';
	putchar(ch);
	ch = 't';
	putchar(ch);
	ch = 'c';
	putchar(ch);
	ch = 'h';
	putchar(ch);
	ch = 'a';
	putchar(ch);
	ch = 'r';
	putchar(ch);
	putchar ('\n');
	return 0;
}
