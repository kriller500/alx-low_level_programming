#include <stdio.h>

/**
 * main - Entry point
 * Return: Always zero (Success)
 *
 */
	int main()
{
	/* this program prints characters*/

	int ch;

	putchar(ch);
	ch = '_';
	putchar(ch);
	ch = 'p';
	putchar(ch);
	ch = 'u';
	putchar(ch);
	ch = 't';
	putchar(ch);
	ch = 'c';
	putchar(ch);
	ch = 'h';
	putchar(ch);
	ch = 'a';
	putchar(ch);
	ch = 'r';
	putchar(ch);
	putchar ('\n');
	/*return 0: always*/
	return (0);
}
